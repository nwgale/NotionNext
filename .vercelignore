# Ignore the entire Next.js cache directory from Vercel deployments.
# This prevents the large `lastmod-map.json` and other cache files
# from being bundled into the serverless function, solving the size limit issue.
.next/cache